import { Footer, Header } from '../../components';
import { Container, Title } from './styles';

const LoginPage = () => {
  return (
    <Container>
      <Header />
      <Title>로그인페이지</Title>
      <Footer />
    </Container>
  );
};

export default LoginPage;
